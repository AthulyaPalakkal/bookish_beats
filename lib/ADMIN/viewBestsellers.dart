import 'package:flutter/material.dart';

class viewBestsellers extends StatelessWidget {
  viewBestsellers({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    body:Column(
      children: [
        Container(
        )
      ],
    ) ,
    );
  }
}
